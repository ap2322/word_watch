import { expect } from "chai"
import sinon from "sinon"

describe("Word watch functions", () => {
  describe("On the word watch root / a user sees", () => {
    it("the top word and it's count on the right side of the page", () => {
      // expect(true).to.be.true;
    })
  })
})
